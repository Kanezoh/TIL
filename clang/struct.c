#include <stdio.h>
#include <string.h>
// 構造体タグ、型ではない
struct student {
  int year;
  int clas;
  int number;
  char name[64];
  double stature;
  double weight;
};
  
int main(void)
{
  struct student data1, data2;
  strcpy(data.name, "MARIO");
  printf("%s\n", data.name);
  return 0;
}
